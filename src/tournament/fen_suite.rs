// use crate::board::STARTPOS_FEN;

// pub fn build_fens() -> Vec<&str> {
//     let mut fens: Vec<&str> = Vec::with_capacity(8);

//     fens.push(STARTPOS_FEN);
//     // fens.push("some fen");

//     fens
// }
